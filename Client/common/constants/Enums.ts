export enum AccountType {
    Athlete = "Athlete",
    Coach = "Coach",
    SignedOut = "SignedOut"
}

export enum ExerciseType {
    Rest = "rest",
    Run = "run",
    Strength = "strength"
}

export enum InputType {
    Run = "run",
    Rest = "rest"
}