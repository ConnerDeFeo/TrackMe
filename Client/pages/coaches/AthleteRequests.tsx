import { Text } from "react-native";
import { View } from "react-native";

const AthleteRequests = () => {
    return (
        <View>
            <Text>Athlete Requests</Text>
        </View>
    );
}

export default AthleteRequests;
